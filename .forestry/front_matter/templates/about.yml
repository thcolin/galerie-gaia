---
label: About
hide_body: true
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
  description: Titre
- name: tabs
  type: field_group_list
  fields:
  - name: title
    type: text
    config:
      required: true
    label: Title
    description: Titre
  - name: content
    type: field_group_list
    fields:
    - name: column
      type: textarea
      default: ''
      config:
        required: true
        wysiwyg: true
        schema:
          format: markdown
      label: Column
      description: Colonne
    config:
      min: 
      max: 
      labelField: column
    label: Content
    description: Contenu
  config:
    min: 
    max: 
    labelField: title
  label: Tabs
  description: Onglets
- name: template
  type: text
  config:
    required: false
  label: Template
  description: Template
  hidden: true
  default: about
pages:
- src/pages/about.md
