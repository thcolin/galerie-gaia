---
label: Home
hide_body: true
fields:
- name: carousel
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 250
    label: Image
    description: Image
  config:
    min: 
    max: 
    labelField: 
  label: Carousel
  description: Carousel
pages:
- src/pages/index.md
