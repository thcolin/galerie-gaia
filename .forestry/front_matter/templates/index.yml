---
label: Home
hide_body: true
fields:
- name: carousel
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
    description: Image
  config:
    min: 
    max: 
    labelField: 
  label: Carousel
  description: Carousel
- name: seo
  type: include
  config: {}
  template: seo
  label: SEO
pages:
- src/pages/index.md
