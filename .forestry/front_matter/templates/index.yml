---
label: Home
hide_body: true
fields:
- name: carousel
  type: field_group_list
  fields:
  - name: work
    type: select
    default: []
    config:
      required: true
      options: []
      source:
        type: pages
        section: works
        file: 
        path: 
    label: Work
    description: Oeuvre
  config:
    min: 
    max: 
    labelField: work
  label: Carousel
  description: Carousel
pages:
- src/pages/index.md
